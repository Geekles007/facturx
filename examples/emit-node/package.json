{
  "name": "example-emit-node",
  "private": true,
  "type": "module",
  "description": "Émission : modèle applicatif (JSON) + PDF → facture Factur-X EN 16931 sur disque.",
  "scripts": {
    "start": "tsx src/index.ts",
    "check": "tsc --noEmit -p tsconfig.json && tsx src/index.ts"
  },
  "dependencies": {
    "facturx": "workspace:*",
    "pdf-lib": "^1.17.1"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "tsx": "^4.20.0",
    "typescript": "^5.9.3"
  }
}
